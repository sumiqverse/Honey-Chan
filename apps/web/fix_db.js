const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const lab = await prisma.user.findFirst({ where: { role: 'LAB' } }); if (!lab) return; await prisma.supplyChainEvent.updateMany({ where: { stage: 'QUALITY_TESTED', actorId: null }, data: { actorId: lab.id } }); await prisma.qualityTest.updateMany({ where: { labId: null }, data: { labId: lab.id } }); console.log('Fixed DB'); await prisma.$disconnect(); } main();
